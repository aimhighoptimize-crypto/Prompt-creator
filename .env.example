# Required: your Anthropic API key. Prompt Architect uses Claude as its
# reasoning/generation engine regardless of which target model a prompt is
# being written for.
ANTHROPIC_API_KEY=

# Optional: port for the local server (defaults to 3000).
PORT=3000
